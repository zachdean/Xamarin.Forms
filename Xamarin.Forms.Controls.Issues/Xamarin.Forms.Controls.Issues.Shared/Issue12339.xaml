<?xml version="1.0" encoding="utf-8" ?>
<controls:TestContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Xamarin.Forms.Controls"
    x:Class="Xamarin.Forms.Controls.Issues.Issue12339"
    Title="Issue 12339">
    <StackLayout>
        <Label
            Padding="12"
            BackgroundColor="Black"
            TextColor="White"
            Text="If you can see the gradient below, the test has passed."/>
        <Frame CornerRadius="12" HeightRequest="100">
            <Frame.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0" Color="{Binding Start}" x:Name="gstop1" />
                    <GradientStop Offset="1" Color="{Binding Stop}" x:Name="gstop2" />
                </LinearGradientBrush>
            </Frame.Background>
            <StackLayout>
                <Label BindingContext="{x:Reference Name=gstop1}" Text="{Binding Color}"/>
                <Label BindingContext="{x:Reference Name=gstop2}" Text="{Binding Color}"/>
            </StackLayout>
        </Frame>
    </StackLayout>
</controls:TestContentPage>