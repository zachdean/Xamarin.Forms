<?xml version="1.0" encoding="utf-8" ?>
<controls:TestContentPage
    xmlns:controls="clr-namespace:Xamarin.Forms.Controls"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Xamarin.Forms.Controls.Issues.Issue11982"
    Title="Issue 11982">
    <ContentPage.Content>
        <StackLayout>
            <Label
                Padding="12"
                BackgroundColor="Black"
                TextColor="White"
                Text="Tap the Button. If not trigger the SelectionChanged, the test has passed."/>
            <CollectionView
                x:Name="Issue11982CollectionView"
                SelectionMode="Single"
                ItemsSource="{Binding Items}"
                SelectionChangedCommand="{Binding SelectionChangedCommand}"
                SelectionChangedCommandParameter="{Binding SelectedItem, Source={x:Reference Issue11982CollectionView}}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.LeftItems>
                                <SwipeItems>
                                    <SwipeItem
                                        BackgroundColor="Red"
                                        Text="SwipeItem" />
                                </SwipeItems>
                            </SwipeView.LeftItems>
                            <Grid BackgroundColor="White" ColumnDefinitions="*,Auto">
                                <Label Grid.Column="0" Text="{Binding Name}" VerticalTextAlignment="Center" Margin="10,0,0,0" />
                                <Button Grid.Column="1" Text="Command" Command="{Binding TestCommand}" />
                            </Grid>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</controls:TestContentPage>